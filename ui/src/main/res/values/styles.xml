<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="dotZeroTitle20" parent="@android:style/TextAppearance">
        <item name="android:fontFamily">@font/lacosta</item>
        <item name="android:textSize">40dp</item>
        <item name="android:textColor">#ffffff</item>
    </style>

    <style name="samuraiTitle20" parent="@android:style/TextAppearance">
        <item name="android:fontFamily">@font/samureix_regular</item>
        <item name="android:textSize">25dp</item>
        <item name="android:textColor">#ffffff</item>
    </style>

</resources>